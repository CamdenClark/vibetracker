{
  "name": "vibetracker",
  "version": "0.1.0",
  "description": "CLI tool that hooks into Claude Code to capture conversation transcripts in SQLite",
  "type": "module",
  "bin": {
    "vibetracker": "./index.ts"
  },
  "scripts": {
    "test": "bun test",
    "db:generate": "bunx drizzle-kit generate",
    "db:migrate": "bunx drizzle-kit migrate",
    "db:push": "bunx drizzle-kit push",
    "db:studio": "bunx drizzle-kit studio"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.1"
  },
  "devDependencies": {
    "bun-types": "latest",
    "drizzle-kit": "^0.31.8"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
